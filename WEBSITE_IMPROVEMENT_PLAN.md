# GateRemoteSource 网站改造执行计划

状态：第一阶段已完成并通过回归；B04、B05、B08、B09 等待站点所有者资料或决策

## 0. 2026-08-23 执行检查点

已完成：

- B00：依赖、当前 Next.js 文档、lint、TypeScript 和静态构建基线已核查。
- B01：询盘交互已改为诚实的 WhatsApp / 邮件草稿流程；删除假附件上传和假成功状态。
- B02：重复 Provider、弹窗语义、焦点恢复、焦点循环、Header ARIA、标题层级和首页颜色对比已修正。
- B03：首页已按产品、买家路径、有限兼容性样本、验证流程、应用、风险、FAQ、CTA 重排；未经确认的统计和工厂图库不再展示。
- B06：西班牙语明显乱码已修正；六套词典键结构检查无缺失或多余键。
- B07：英文首页元数据已压缩；未经核实的文章日期不再公开展示或写入结构化数据 / sitemap；文章 CTA 按主题预选。
- B10：216 个静态页面构建通过；导出产物 276 个 HTML、站内坏链 0；390×844 与 1440×1000 无横向溢出；Lighthouse 为 Performance 90、Accessibility 100、Best Practices 100、SEO 100；完整 `npm audit` 为 0。

额外完成：Next.js 与 `eslint-config-next` 从 16.2.4 升级到 16.3.2，修复当时依赖审计发现的生产和开发依赖漏洞；导出 HTML 与客户端语言切换都会维护正确的 `<html lang>`。

仍然阻塞：

- B04：需要 D1 公司身份和 D5 经审核法律文本。
- B05：需要 D4 真实产品资料；当前只有产品类别，不应伪造 SKU 详情页。
- B08：需要 D3 托管、表单服务、附件、隐私和 CRM 决策；当前站点只打开 WhatsApp 或邮件草稿，不会在网站内接收数据。
- B09：需要 D6 可追溯真实图片；质量页已暂时移除未确认工厂图库，现有媒体后续仍需逐张核验和替换。

本检查点未提交、未推送、未部署。

基线提交：`e364641`

目标分支：`main`（执行模型不得自行建分支、提交、推送或部署）

主要目标：在不编造公司、产品、认证和工厂信息的前提下，提高产品可见度、询盘完成率、供应商可信度、多语言质量和技术可维护性。

## 1. 本计划的边界

本轮可以做：

- 修正误导性的询盘和附件交互。
- 调整首页内容顺序，让产品和采购信息先出现。
- 恢复或改造现有产品分类模块，不生成新图片。
- 删除无法证明的弱数据或把它们改成普通说明。
- 增加清晰的买家入口：批发商、安装商、OEM 客户。
- 修正明显的西班牙语乱码、无障碍、标题层级和 SEO 元数据问题。
- 整理公司页、产品页、法律页和询盘后端的实施接口。
- 保留现有 URL，避免破坏已有搜索收录。

本轮不做：

- 不生成、伪造或美化工厂照片、产品照片、证书、检测报告和客户评价。
- 不把未经确认的数据写成事实，包括产能、成立年份、客户数量、认证、交期和合格率。
- 不自行选择表单服务、云存储、CRM、统计工具或新托管平台。
- 不添加密钥、令牌、`.env` 内容、分析代码或未经批准的外部网络请求。
- 不随机修改博客发布日期来制造“持续更新”的假象。
- 不自行提交、推送或部署。

真实图片由站点所有者后续提供。图片到位前，工厂区域应采用真实可核验的文字流程，或暂时隐藏图库，不能继续把未确认图片称为 “Factory Evidence”。

## 2. 已确认的技术事实

- 项目是 Next.js `16.2.4`，使用 App Router。
- `next.config.mjs` 设置了 `output: 'export'`，当前产物是纯静态网站。
- 当前部署在 GitHub Pages，运行时没有 Next.js API Route、Server Action 或服务端文件上传能力。
- 所有本地化页面已经由 `src/app/[locale]/layout.tsx` 包裹 `LeadModalProvider`，部分子页面又重复包了一层。
- 首页存在 `ProductCategoriesSection`、`QualitySection`、`ResourcesSection` 等组件，但当前首页没有使用其中部分组件。
- 首页公开矩阵只有 4 条兼容性记录，兼容性详情数据只有 6 个品牌。
- `LeadModal.tsx` 只保存附件文件名，未上传文件；邮件按钮使用 `mailto:`，WhatsApp 按钮只打开外部页面。
- `src/i18n/es.ts` 存在大量缺失重音符号和明显拼写问题。
- 当前 Git 工作区在制定本计划前是干净的。

任何执行模型开始写代码前，必须重新核查这些事实，不得把本计划当作永远正确的代码快照。

## 3. 开工前需要站点所有者提供或决定的内容

以下内容没有答案时，相关批次必须暂停，不能让模型猜：

### D1：公司身份

- 法定英文公司名。
- 公司与工厂的真实关系：自有工厂、控股工厂、合作工厂或贸易公司。
- 可公开的完整地址。
- 成立年份、营业执照或注册信息是否可以公开。
- 联系人姓名、职位、工作语言和正常回复时间。
- 可公开的第三方身份链接，例如 LinkedIn、Alibaba、Made-in-China、展会主页。

### D2：可验证商业数据

- 可公开的 SKU 数量，是否能证明“约 1,000”。
- 各产品类别的真实 MOQ。
- 样品准备时间与寄出时间的区别。
- 批量生产交期、付款方式、质保和缺陷处理原则。
- 可公开的认证、报告或合规支持范围。

### D3：询盘接收方式

- 保留 GitHub Pages，还是迁移到支持服务端函数的平台。
- 若保留静态托管，选择哪家经过审核的表单/文件上传服务。
- 询盘发送到哪个邮箱或 CRM。
- 附件允许的类型、大小、保留时间和删除规则。
- 是否允许添加防垃圾服务、转化统计或访问分析。

### D4：产品资料

- 首批公开展示的 12–20 个产品或产品系列。
- 每个产品的内部编号、频率、编码类型、按钮数、电池、MOQ、OEM 选项和适用市场。
- 哪些品牌和型号可以公开写，哪些只能在私下目录中提供。
- 哪些数据已经过工程验证，哪些只能写 “需确认”。

### D5：法律文本

- 隐私政策和使用条款的责任主体。
- 数据处理、附件保存和第三方服务说明。
- 法律文本是否经过专业人士审核。

### D6：真实图片（延后）

- 首页主视觉、代表性产品、工厂、测试、包装和出货原图。
- 每张图的真实拍摄对象、场景和可公开说明。
- “老化测试”“射频测试”“功能测试”等标签必须由懂设备的人确认。

## 4. 给低模型的执行纪律

每次只执行一个批次。一个批次结束并通过检查后，才能开始下一批。

执行规则：

1. 先读 `AGENTS.md`、`CLAUDE.md` 和本计划。
2. 开始前运行 `git status --short --branch`，发现用户改动时先说明，不能覆盖。
3. 编辑前列出本批次涉及的文件和 3–6 条计划。
4. 先用 `rg` 搜索现有实现，不发明组件、路由、字段或配置。
5. 文件编辑使用 `apply_patch`。
6. 没有站点所有者确认的数据，不得补占位事实；可以隐藏模块或保留可选字段。
7. 不生成图片，不替换真实素材，不把现有图片重新描述成未经确认的场景。
8. 安装依赖后，写代码前先搜索并阅读 `node_modules/next/dist/docs/` 中与静态导出、元数据或路由相关的当前版本文档。
9. 每批至少运行 `npm run lint`；涉及路由、元数据或构建配置时还要运行 `npm run build`。
10. 每批结束展示 `git diff --stat`、关键验证结果和未解决问题，然后停止。

复杂度标记：

- `S`：低模型可独立执行。
- `M`：低模型可执行，但必须严格限定文件并做浏览器复核。
- `H`：涉及后端、法律或托管决策，需要更强模型或人工逐行审查。

## 5. 目标页面结构

首页建议顺序：

1. 首屏：明确产品、目标买家、一个主 CTA、一个兼容性入口。
2. 产品类别：先让买家看到实际卖什么、基本参数和 MOQ 规则。
3. 买家路径：批发商、安装商/锁匠、OEM/Private Label。
4. 兼容性参考：明确这是有限公开参考，不伪装成完整数据库。
5. 样品验证流程：买家需要提供什么、供应商返回什么。
6. 质量与供应证据：先用可验证流程；真实图片到位后再加入图库。
7. 公司与商业条件：主体、地点、回复时间、MOQ、交期和售后边界。
8. FAQ 和最终询盘入口。
9. 商标和兼容性免责声明放在靠后位置及品牌详情页，不占据首页前半段。

视觉方向：保留现有深蓝、白色和橙色体系；减少小卡片和重复标签；优先使用产品、参数、流程和真实证据，不靠装饰网格、阴影或图标制造“专业感”。

## 6. 执行批次

### B00：建立基线和执行检查（S）

目标：确保后续每个批次都从可构建状态开始。

只读或生成依赖，不修改业务代码。

步骤：

1. 运行 `git status --short --branch`。
2. 运行 `npm ci`。
3. 用 `rg --files node_modules/next/dist/docs` 找到与 static export、metadata、route conventions 有关的当前文档并阅读。
4. 运行 `npm run lint`。
5. 运行 `npm run build`。
6. 记录现有警告和失败，不把历史问题混进后续批次。

验收：

- 能明确区分基线问题和新改动问题。
- 不修改源码。
- 不提交 `node_modules`、`.next` 或 `out`。

### B01：先修正误导性的询盘交互（S）

目标：在没有真实后端前，让用户清楚知道按钮会做什么，杜绝“附件已经上传”的误解。

主要文件：

- `src/components/LeadModal.tsx`
- `src/components/BlogCommentBox.tsx`
- `src/i18n/en.ts`
- `src/i18n/es.ts`
- `src/i18n/fr.ts`
- `src/i18n/it.ts`
- `src/i18n/pt.ts`
- `src/i18n/ru.ts`

实施要求：

1. 在真实上传后端完成前，删除附件选择器和文件名列表。
2. 改成明确提示：打开 WhatsApp 后在那里添加照片；打开邮件客户端后在那里添加附件。
3. WhatsApp 入口不能为了打开聊天而强制买家填写邮箱。
4. 表单至少收集或引导买家填写：品牌/型号、频率、目标国家、预估数量；公司名和邮箱可按渠道决定是否必填。
5. 按钮文字必须体现动作，例如 “Continue in WhatsApp” 和 “Open Email Draft”，不能写成已经提交成功。
6. 不显示虚假的成功状态；只有真正后端确认后才能显示 “Inquiry received”。
7. 把当前博客 “Leave a comment” 改成 “Email Eric an RF question” 或同等真实含义，因为它不是公开评论系统。
8. 消息内容可以加入当前页面 URL 和语言，方便销售判断来源；不得加入跟踪代码。

验收：

- 页面上不存在可选择但无法发送的附件控件。
- 不填邮箱也能进入 WhatsApp。
- 邮件按钮明确说明会打开本机邮件客户端。
- 用户取消外部应用后，网站不声称已收到询盘。
- 六种语言的键结构一致。
- `npm run lint` 和 `npm run build` 通过。

### B02：清理重复 Provider 和基础无障碍问题（S）

目标：减少重复状态容器，修正 Lighthouse 已发现的基础问题，不改变页面业务行为。

主要文件：

- `src/app/[locale]/layout.tsx`
- `src/app/[locale]/page.tsx`
- `src/app/[locale]/compatibility/page.tsx`
- `src/app/[locale]/compatibility/[brand]/page.tsx`
- `src/app/[locale]/blog/page.tsx`
- `src/components/Footer.tsx`
- `src/components/Header.tsx`
- `src/app/globals.css`

实施要求：

1. 本地化路由统一使用 `[locale]/layout.tsx` 中的 `LeadModalProvider`，移除子页面重复 Provider。
2. 不删除非本地化旧路由所需的 Provider；先确认 `LegacyEnglishShell` 和根路由行为。
3. 修正页脚标题跳级，使用连续语义层级或带 `aria-label` 的普通文本。
4. 提高浅色背景上的橙色、浅灰色小字和语言切换按钮对比度。
5. 10px/11px 文字只用于非关键信息；表单标签、导航和 CTA 保证可读。
6. 保持键盘焦点可见，弹窗打开后应聚焦首个可操作元素，关闭后恢复触发按钮焦点。
7. 不在本批次重做视觉设计。

验收：

- 同一页面 DOM 中只有一个询盘弹窗实例。
- Header、Footer、弹窗可用键盘完成主要操作。
- Lighthouse 不再报告现有的 heading-order 错误。
- 关键文字达到 WCAG AA 对比度。
- 桌面与移动页面视觉没有明显回归。

### B03：首页商业结构调整（M）

目标：让买家先看到产品和采购信息，再看到法律说明与通用宣传。

主要文件：

- `src/app/[locale]/page.tsx`
- `src/components/HeroSection.tsx`
- `src/components/Header.tsx`
- `src/components/ProductCategoriesSection.tsx`
- `src/components/StatsSection.tsx`
- `src/components/CapabilityHighlightsSection.tsx`
- `src/components/BrandCompatibilitySection.tsx`
- `src/components/RiskControlSection.tsx`
- `src/components/FactoryEvidenceSection.tsx`
- `src/components/CtaSection.tsx`
- `src/data/homepage.ts`
- 六个语言词典

实施要求：

1. 首屏保留一个主 CTA：发送型号/询盘；次 CTA 指向产品或兼容性参考。
2. 不改变已确认的产品定位，不自行新增 “best”“leading”“top factory” 等无法证明的词。
3. 恢复或重做产品类别区域。没有真实产品图时，采用干净的文字、参数和列表布局，不生成占位图片。
4. Header 增加明确的 Products 入口，指向首页产品区域或产品列表页。
5. 删除或暂时隐藏 `Multi`、`Mainstream`、`OEM` 这类不能作为数字证据的统计项。
6. “约 1,000 SKU”“24–48h”只有在 D2 确认后才能保留；否则删除或改成不带数字的事实说明。
7. 把买家入口明确成三条：Distributor/Wholesaler、Installer/Locksmith、OEM/Private Label，并分别连接现有的 catalog、compatibility、oem-odm 页面。
8. 兼容性搜索明确标注为 sample/public reference；不能让 4 条静态数据看起来像完整数据库。
9. `RiskControlSection` 移到页面后部，标题改成买家结果，例如降低错配、退货和地区版本风险；法律免责声明保留在页脚和品牌页。
10. 图片未确认前，隐藏 “Factory Evidence” 图库或改成不依赖图片的质量流程说明。
11. 每个区域只完成一个任务，删除重复的 OEM、全球运输和工程确认文案。

验收：

- 首屏能在 5 秒内回答卖什么、卖给谁、下一步是什么。
- 首页前半部分能看到产品类别和基本采购信息。
- 页面不显示未经确认的数量、交期、认证或工厂身份。
- 三类买家都有独立入口。
- 390×844 和 1440×1000 两个视口无横向溢出，首屏 CTA 可见。
- `npm run lint`、`npm run build` 通过。

### B04：公司身份、About、Contact 和法律页（H，等待 D1/D5）

目标：让买家和搜索引擎能核验责任主体。

建议文件：

- 新增 `src/data/company.ts`
- 新增 `src/app/[locale]/about/page.tsx`
- 新增 `src/app/[locale]/contact/page.tsx`
- 新增 `src/app/[locale]/privacy/page.tsx`
- 新增 `src/app/[locale]/terms/page.tsx`
- 按现有旧路由架构补充必要的英文入口
- `src/components/Header.tsx`
- `src/components/Footer.tsx`
- `src/lib/seo.ts`
- `src/app/sitemap.ts`
- `src/app/[locale]/sitemap.ts`

实施要求：

1. 所有公司事实从一个数据文件读取，避免页脚、结构化数据和 About 页面互相矛盾。
2. “Manufacturer” 只能在 D1 确认工厂关系后使用；否则采用准确的 supplier、manufacturer partner 或实际身份。
3. Organization JSON-LD 增加真实地址、联系人和经过确认的 `sameAs`；删除不适合作为企业背书的链接。
4. 法律页不得由模型凭空生成并直接发布。可以根据确认内容实现页面结构，但正文必须由站点所有者或法律专业人士审核。
5. 未经审核的翻译版法律页不要自动建立 hreflang；可以先只发布英文权威版本。
6. 不在公开页面展示营业执照号码、身份证件或私人地址，除非站点所有者明确批准。

验收：

- Header/Footer 中的 About、Contact、Privacy、Terms 链接都返回 200。
- 公司名称、地址、联系方式和结构化数据一致。
- 页面没有 `TODO`、示例地址或虚构年份。
- sitemap、canonical 和 hreflang 与真实发布范围一致。

### B05：公开产品结构和产品详情页（M，等待 D4）

目标：用有限但真实的产品资料支撑“供应能力”，完整目录仍可申请获取。

建议实现：

- 新增强类型产品数据模型。
- 新增 `src/app/[locale]/products/page.tsx`。
- 新增 `src/app/[locale]/products/[slug]/page.tsx`。
- 增加必要的英文旧入口。
- Header、Footer、sitemap 增加产品入口。

每个产品至少需要：

- 内部产品编号或系列编号。
- 产品类别。
- 频率。
- 编码/协议类型。
- 按钮数、电池或供电方式。
- 兼容性确认状态。
- MOQ 与 OEM 范围。
- 目标市场或地区限制。
- 样品验证要求。
- 真实图片路径（图片未到位时字段允许为空，页面不得显示假图）。

实施要求：

1. 产品数据与兼容性品牌数据分开管理，不把品牌名等同于已验证兼容。
2. 产品页的 CTA 自动带入产品编号和来源 URL。
3. Product JSON-LD 只用于真实、公开、可识别的产品；没有公开价格时不要编造 Offer。
4. 保留 “Request Full Catalog” 作为完整目录入口，但公开页必须足以让买家判断供应范围。

验收：

- 公开产品页没有空卡片、假图、虚构价格或不确定兼容性断言。
- 任一产品都能直接生成带产品编号的询盘内容。
- 无图片时布局仍完整。
- 新路由进入 sitemap，404 页面不进入 sitemap。

### B06：西班牙语修复和多语言一致性（M）

目标：消除明显乱码，保证六套词典结构一致；不声称完成母语级翻译。

主要文件：

- `src/i18n/es.ts`
- `src/i18n/en.ts`
- `src/i18n/fr.ts`
- `src/i18n/it.ts`
- `src/i18n/pt.ts`
- `src/i18n/ru.ts`
- `src/i18n/dictionaries.ts`
- 可新增词典键一致性测试

实施要求：

1. 先以 `en.ts` 为键结构基准，检查每种语言缺失、多余和类型不一致的键。
2. 修复西班牙语中已确认的错误，例如 `Fbrica`、`Cmo`、`Pginas`、`Informacin`、`multimerado` 等。
3. 不用机械替换全文件；逐条检查上下文，避免破坏代码和品牌名。
4. 非英文博客当前若没有真实翻译，继续保持 `noindex`，不要自动索引英语正文的多语言壳。
5. 是否暂时 noindex 未经人工审核的服务页，需要站点所有者根据 Search Console 数据决定，执行模型不能自行下线索引。
6. 法律文本单独审核，不与普通营销文案一起机器翻译。

验收：

- 六套词典键结构一致。
- 西班牙语页面不存在已知缺重音问题。
- 语言切换保留当前路径，未知路径有可预期回退。
- 每种语言抽查首页、兼容性、OEM、工厂质量、目录和博客页。

### B07：SEO、文章日期和内容卫生（M）

目标：保留现有收录，减少批量内容和错误元数据带来的信任问题。

主要文件：

- `src/lib/seo.ts`
- `src/app/page.tsx`
- `src/app/[locale]/layout.tsx`
- `src/app/sitemap.ts`
- `src/app/[locale]/sitemap.ts`
- `src/data/blog.ts`
- `src/data/author.ts`
- 博客页面组件

实施要求：

1. 英文首页 title 从当前 82 字符压缩到约 55–65 字符，保留主产品和 B2B 意图。
2. description 控制在约 140–160 字符，不堆叠同义关键词。
3. `siteUpdatedAt` 使用真实内容更新时间，不在每次构建时伪造为今天。
4. 16 篇同日文章不得随机分散日期。需要提供真实首发日期；没有真实日期时可以不展示日期，不能编造。
5. 保留已有文章 URL，删除或合并内容前先查 Search Console 的曝光、点击和外链。
6. 优先保留与批发、OEM、匹配、质量和认证决策直接相关的文章。
7. DIY 和泛智能家居文章若没有商业转化，应降低推荐权重，不必立即删除。
8. 每篇文章 CTA 根据主题带入询盘类型，不能全部指向同一个泛化按钮。
9. 作者履历、经验年限和头像只使用可证明信息，并连接到 About 页面。
10. 修复 sitemap 中失配的更新时间和不存在的路径。

验收：

- 已收录 URL 不发生无计划变化。
- canonical、hreflang、robots 和 sitemap 互相一致。
- 元数据不重复、不超长、不包含无法证明的词。
- 文章日期可解释且不伪造。

### B08：真正的询盘后端和附件上传（H，等待 D3）

目标：让询盘真正进入可追踪的收件系统。

先选择一种架构：

- 方案 A：继续 GitHub Pages，使用经过审核、支持 multipart 文件和 webhook 的外部表单服务。
- 方案 B：迁移到支持服务端函数的平台，用服务端接口处理验证、附件、通知和存储。

如果询盘是核心业务，优先评估方案 B；但托管迁移必须由站点所有者批准。

服务端最低要求：

- 服务端验证必填字段、邮箱格式、文件 MIME 和文件大小。
- 限制提交频率并处理垃圾询盘。
- 附件使用私有存储，不公开可猜 URL。
- 密钥只放环境变量。
- 明确数据保留和删除规则。
- 成功后返回询盘编号；失败时保留用户已填内容并允许重试。
- 邮件通知失败不能导致询盘数据丢失。
- 记录最少必要的来源页面和语言；未批准前不加访客跟踪。

前端最低要求：

- 只有服务端确认后显示成功。
- 上传过程显示进度、大小和错误。
- WhatsApp 与 Email 继续作为备用渠道。
- 文件为空时也可提交普通询盘。
- 不把网络错误伪装成成功。

验收：

- 用测试邮箱完成一次真实提交。
- 验证附件确实可由授权人员读取。
- 测试超大文件、错误类型、网络失败、重复提交和垃圾提交。
- 仓库和构建日志中没有密钥。
- 隐私政策与实际数据流一致。

### B09：真实图片替换（M，等待 D6）

目标：只使用真实、标签准确、能帮助采购判断的图片。

执行要求：

1. 站点所有者先提供原图和每张图的真实说明。
2. 为首页、产品、工厂、测试、包装和出货建立明确文件名，不覆盖含义不同的旧图。
3. 转为合适尺寸的 WebP，同时保留必要的原始资料备份在网站仓库之外。
4. alt 文本描述画面事实，不塞关键词。
5. “Aging Test Room”“RF Testing”“SMT Line” 等专业标签必须人工确认。
6. 图片到位后再恢复或增强 Factory Evidence 区域。
7. 删除被替换的未确认图片前，先列出准确路径并取得站点所有者确认。

验收：

- 页面上的每张工厂图都有可追溯来源和准确标签。
- 移动端不加载不必要的超大桌面图。
- 图片不含无法公开的客户标签、订单号、员工隐私或设备密码。

### B10：全站回归与发布前检查（M）

每个实施阶段完成后执行，正式发布前再执行一次。

命令检查：

```bash
npm ci
npm run lint
npm run build
git diff --check
git status --short --branch
```

页面检查：

- `/en`
- `/en/compatibility`
- 6 个真实品牌详情页
- `/en/oem-odm`
- `/en/factory-quality`
- `/en/request-catalog`
- `/en/blog`
- 至少 3 篇文章
- 新增的 products/about/contact/privacy/terms 页面（如果该批已完成）
- `/robots.txt`
- `/sitemap.xml`

浏览器检查：

- 桌面：1440×1000。
- 移动：390×844。
- 键盘导航、弹窗打开/关闭、语言切换、产品 CTA、WhatsApp、邮件和真实表单。
- 控制台无错误。
- 无横向滚动、遮挡或不可见 CTA。

目标指标：

- Lighthouse Performance 不低于当前基线 90。
- SEO 保持 100 或说明分数下降原因。
- Accessibility 从 94 提升到至少 98。
- LCP 目标低于 2.5 秒；若 GitHub Pages 缓存限制导致未达标，单独记录，不用破坏功能换分数。

链接检查：

- sitemap 中每个 URL 返回 200。
- 页面可见链接不指向 404。
- 不把未实现的 Alutech、Beninca、Genius、Marantec 页面加入公开导航或 sitemap。
- 旧英文入口继续工作或有明确重定向。

发布规则：

- 未经站点所有者明确要求，不提交、推送或部署。
- 部署前展示变更文件、验证结果、已知限制和可回滚提交。

## 7. 推荐执行顺序

低模型现在可以按此顺序做：

1. `B00` 基线检查。
2. `B01` 诚实的询盘交互。
3. `B02` Provider、无障碍和基础清理。
4. `B03` 首页商业结构调整。
5. `B06` 西班牙语和词典一致性。
6. `B07` SEO 与内容卫生中不依赖 owner 数据的部分。
7. `B10` 阶段回归。

以下批次等待输入：

- `B04` 等待 D1/D5。
- `B05` 等待 D4。
- `B08` 等待 D3，并建议更强模型或人工严格审查。
- `B09` 等待 D6。

## 8. 可直接复制给低模型的通用提示词

```text
请先完整阅读 AGENTS.md、CLAUDE.md 和 WEBSITE_IMPROVEMENT_PLAN.md。

只执行批次 BXX，不要顺手处理其他批次。开始前运行 git status --short --branch，并用 3–6 条说明本批要修改的文件和计划。如果存在未提交改动，先报告，不要覆盖。

不要编造公司、产品、工厂、认证、交期、MOQ、客户、评价、API、配置或文件路径；不要生成或替换图片；不要添加分析、遥测或未经批准的网络服务。使用 apply_patch 编辑。遵守现有 Next.js 16 静态导出架构，写代码前先搜索当前实现和本地 Next.js 文档。

完成后运行本批要求的 lint/build/页面检查，展示 git diff --stat、验证结果、未解决问题和本批修改的文件，然后停止。不要提交、推送或部署。
```

## 9. 分批提示词

### 执行 B01

```text
执行 WEBSITE_IMPROVEMENT_PLAN.md 的 B01。重点是删除假附件上传，明确 WhatsApp 和 mailto 的真实行为，减少无意义必填，并把博客评论改成真实的邮件提问。不要接入后端服务，不要添加新依赖。完成后检查六个语言词典键一致，并运行 npm run lint 与 npm run build。
```

### 执行 B02

```text
执行 WEBSITE_IMPROVEMENT_PLAN.md 的 B02。只清理本地化页面中重复的 LeadModalProvider、修正标题层级、颜色对比、焦点和键盘行为。不要重做页面视觉，不要修改文案策略。完成后在 390×844 和 1440×1000 下复核首页和弹窗，并运行 lint/build。
```

### 执行 B03

```text
执行 WEBSITE_IMPROVEMENT_PLAN.md 的 B03。先列出新的首页区域顺序，再做小范围组件复用。不要生成图片；真实图片缺失时使用无图片的文字/参数布局。删除未经确认的统计，不得发明替代数字。保留现有 URL 和蓝橙视觉体系。完成后做桌面、移动端和 build 验证。
```

### 执行 B06

```text
执行 WEBSITE_IMPROVEMENT_PLAN.md 的 B06。先检查六套词典键结构，再修西班牙语明显乱码和缺重音。不要用全局正则盲目替换，不要自行改变索引策略。输出修复过的词条范围、仍需母语审核的内容和 lint/build 结果。
```

### 执行 B07

```text
执行 WEBSITE_IMPROVEMENT_PLAN.md 的 B07 中不依赖 owner 数据的部分。可以缩短元数据、修 sitemap 更新时间逻辑和 CTA 相关性；不得随机修改文章日期，不得删除已有 URL，不得改作者履历。任何需要 Search Console 或真实日期的数据都列为阻塞项。
```

## 10. 完成定义

网站完成这轮改造，不以“页面更漂亮”为标准，而以以下事实为标准：

- 买家能快速看到实际供应范围和下一步动作。
- 网站不再让用户误以为附件或询盘已经发送。
- 不展示无法证明的数字、身份、认证或工厂证据。
- 三类主要买家能进入不同路径。
- 产品、公司和联系方式可以核验。
- 六种语言结构一致，西班牙语没有明显乱码。
- 现有搜索 URL、canonical、hreflang 和 sitemap 保持稳定。
- lint、build、移动端、桌面端、键盘和关键询盘流程全部通过。
